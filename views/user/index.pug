h1 Users List

form(action="/users/create", method="GET")

  button Add User

form(action="/users/search", method="GET")

  input(type="text", name= "q")
    
  button Search
//
  ul
    each user in users
      li
        p Name:
     //TẠO KHOẢN CÁCH
      span &nbsp; 
      a(href='/user/update/' + book.id) Update
      span &nbsp; 
      a(href='/user/delete/' + book.id) Delete